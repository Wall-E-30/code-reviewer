version: '3.8'
services:
  aion-engine:
    build: .
    image: aion-code-reviewer:latest
    container_name: aion-engine
    ports:
      - "7860:7860"
    environment:
      - HF_TOKEN=${HF_TOKEN:-your_hf_token_here}
      - MODEL_NAME=Qwen/Qwen2.5-72B-Instruct
    restart: unless-stopped
